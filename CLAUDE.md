# CLAUDE.md

## 作業継続プロトコル(5時間制限対策)— 必須

このリポジトリでの作業は、Claude の 5 時間使用制限にいつ到達しても失われないように、
以下のプロトコルに常に従うこと。

### 1. こまめなチェックポイント

- **サブタスクを 1 つ完了するごと**、または **20〜30 分相当の作業ごと** に必ずチェックポイントを取る。
- チェックポイント = 以下の 2 つをセットで行う:
  1. `.claude/session-state.md` を現在の状態に更新する(現在のタスク / 完了した作業 / 次のステップ / メモ)
  2. 変更をコミットして作業ブランチへプッシュする(プッシュ失敗時は 2s, 4s, 8s, 16s で最大 4 回リトライ)
- ユーザーが `/checkpoint` を実行した場合も同じ手順を行う。

### 2. 制限が近い・作業を中断するとき

- 大きな作業の途中でも、**中断する前に必ずチェックポイントを取る**。未コミットの状態で放置しない。
- `.claude/session-state.md` の「次のステップ」には、**前提知識ゼロの新しいセッションが読んでそのまま作業を再開できる粒度**で書く
  (対象ファイル、行番号、やりかけの変更内容、残りの手順、既知の問題)。
- リモート(Web)セッションで `send_later` ツールが使える場合は、制限リセット後の時刻に
  「`.claude/session-state.md` を読んで作業を再開して」というメッセージを予約してから終了する。

### 3. セッション開始時(再開)

- セッション開始フックが `.claude/session-state.md` の内容を表示する。
  「次のステップ」が残っている場合は、ユーザーの指示を待つ前にまずその内容を把握し、
  再開を求められたら即座に続きから着手する。
- ユーザーが `/resume` を実行した場合は、`.claude/session-state.md` を読んで残作業を再開する。
- タスクがすべて完了したら、`.claude/session-state.md` の「現在のタスク」を「なし(完了)」に更新してコミットする。

## プロジェクト概要

キャリアコンサルタント試験対策のクイズアプリ。`index.html` 単体で動作する静的サイト
(HTML/CSS/JavaScript がすべて 1 ファイルに含まれる)。

- 実行方法: `index.html` をブラウザで開くだけ。ビルド・依存関係なし。
- テスト: なし。変更後はブラウザでの動作確認を行う。
